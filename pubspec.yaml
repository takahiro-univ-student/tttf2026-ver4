name: island_ai
description: Island navigation AI prototype (local run)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: "^3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/island_spots.csv
    - assets/data/events.csv
    - assets/data/kids_quiz.csv